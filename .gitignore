# macOS / editor noise
.DS_Store
**/.DS_Store
*.swp
*.swo
*~

# Python caches / local environments
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
.venv-*/
venv/
env/

# Large local input datasets
data/

# Generated pipeline artifacts, model outputs, datasets, previews, and reports
artifacts/

# Pipeline artifacts
# Keep artifacts trackable so official models, maps, metadata, and references
# can be committed. Only volatile run outputs stay local.
artifacts/**/runs/
artifacts/**/workbench_ui/
artifacts/**/logs/
artifacts/**/*.log

# Nested legacy application repository; manage it from its own git repo.
OldSoftwareEsiBuilder/

# Runtime logs and temporary outputs
*.log
tmp/
temp/
